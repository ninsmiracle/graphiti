# Graphiti 社区系统架构解析

## 概述

Graphiti的社区系统是一个基于图数据库的智能社区检测和维护系统。它使用标签传播算法进行社区检测，结合LLM生成社区摘要，并支持增量更新以保持社区信息的实时性。

## 核心概念

### 1. 社区节点 (CommunityNode)
- **作用**: 代表一组紧密相关的实体节点的聚合
- **属性**:
  - `name`: 社区的简短描述性名称
  - `summary`: 社区的详细摘要（由成员实体摘要聚合而成）
  - `group_id`: 所属的组ID
  - `name_embedding`: 用于语义搜索的向量

### 2. 社区边 (CommunityEdge)
- **类型**: `HAS_MEMBER`
- **方向**: `(Community)-[:HAS_MEMBER]->(Entity)`
- **作用**: 连接社区节点和其成员实体节点

### 3. 邻居关系 (Neighbor)
- **作用**: 在标签传播算法中表示节点间的连接关系
- **属性**:
  - `node_uuid`: 邻居节点的UUID
  - `edge_count`: 与该邻居的边数量（权重）

## 核心流程

### 一、全量构建流程 (build_communities)

用于初始化或大规模重建，完整流程如下：

```
1. 社区检测 (get_community_clusters)
   ↓
   1.1 查询所有group_id
   ↓
   1.2 对每个group分别处理：
       - 获取该group的所有实体节点
       - 构建邻居投影图（节点→邻居列表）
       - 执行标签传播算法
       - 返回检测到的社区集群
   ↓
2. 并发构建社区节点 (build_community)
   ↓
   2.1 提取所有实体的摘要
   ↓
   2.2 二叉树归并合并摘要：
       - 两两配对并发调用LLM合并
       - 重复直到只剩一个摘要
   ↓
   2.3 生成社区名称（LLM提取）
   ↓
   2.4 创建CommunityNode对象
   ↓
   2.5 创建HAS_MEMBER边
   ↓
3. 保存到数据库
```

**时间复杂度**: O(log n)轮LLM调用，每轮并发处理

### 二、增量更新流程 (update_community)

用于日常维护，当实体节点添加或更新时调用：

```
1. 确定社区归属 (determine_entity_community)
   ↓
   1.1 检查实体是否已有社区？
       - 是 → 返回该社区 (is_new=False)
       - 否 → 继续
   ↓
   1.2 查询邻居的社区
   ↓
   1.3 统计投票（每个社区的邻居数量）
   ↓
   1.4 选择票数最多的社区 (is_new=True)
   ↓
2. 合并摘要
   ↓
   2.1 将实体摘要与社区摘要合并（LLM）
   ↓
   2.2 生成新的社区名称（LLM）
   ↓
3. 更新成员关系
   ↓
   3.1 如果是新成员，创建HAS_MEMBER边
   ↓
4. 更新embedding
   ↓
   4.1 基于新名称重新生成name_embedding
   ↓
5. 保存社区节点到数据库
```

**优势**: 只需2次LLM调用，效率远高于全量重建

### 三、标签传播算法 (label_propagation)

社区检测的核心算法，无需LLM：

```
初始化：
  每个节点 → 独立社区（用序号标识）

迭代传播：
  while 有节点改变社区:
    for 每个节点:
      1. 统计邻居所属的各个社区的权重
      2. 选择权重最大的社区
      3. 更新节点的社区归属
      
收敛：
  当一轮迭代中没有节点改变社区时结束

输出：
  将相同社区ID的节点分组返回
```

**特点**:
- 基于局部信息的贪心算法
- 考虑边的权重（edge_count）
- 通常快速收敛（几轮迭代）
- 无需预设社区数量

## 关键函数详解

### 1. get_community_clusters()
**作用**: 社区检测的入口函数

**输入**: 
- driver: 图数据库驱动
- group_ids: 要处理的组ID列表（None表示处理所有）

**输出**: 
- List[List[EntityNode]]: 社区集群列表

**流程**:
1. 查询所有group_id（如果未指定）
2. 对每个group构建邻居投影图
3. 执行标签传播算法
4. 获取完整的EntityNode对象

### 2. label_propagation()
**作用**: 使用标签传播算法检测社区

**输入**: 
- projection: {node_uuid: [Neighbor列表]}

**输出**: 
- List[List[str]]: UUID列表的列表

**决策逻辑**:
```python
if 候选社区权重 > 1:
    采用候选社区
else:
    保持当前社区或选择较大标签（打破平衡）
```

### 3. build_community()
**作用**: 从实体集群构建单个社区节点

**算法**: 二叉树归并

**流程**:
```
n个摘要
  ↓ 第1轮: n/2次并发LLM调用
n/2个摘要
  ↓ 第2轮: n/4次并发LLM调用
n/4个摘要
  ↓ ...
1个摘要（社区摘要）
```

**时间复杂度**: O(log n)轮

### 4. determine_entity_community()
**作用**: 确定实体应该属于哪个社区

**策略**:
1. 优先：实体已有社区 → 直接返回
2. 次之：邻居投票 → 选择票数最多的社区
3. 最后：无邻居有社区 → 返回None

**返回值**:
- `(CommunityNode, False)`: 已有社区
- `(CommunityNode, True)`: 新加入社区
- `(None, False)`: 无合适社区

### 5. update_community()
**作用**: 增量更新社区（核心维护函数）

**调用时机**:
- 添加新实体节点后
- 更新实体节点后
- 周期性维护

**与全量重建的对比**:
| 维度 | 全量重建 | 增量更新 |
|------|---------|---------|
| LLM调用次数 | O(n log n) | O(1) 固定2次 |
| 适用场景 | 初始化/大规模重构 | 日常维护 |
| 社区一致性 | 全局最优 | 局部最优 |
| 性能 | 慢 | 快 |

## 数据流向

### 写入流程
```
EntityNode (新增/更新)
  ↓
update_community()
  ↓
determine_entity_community()
  ├─ 查询现有社区关系
  └─ 查询邻居社区（投票）
  ↓
summarize_pair() [LLM调用1]
  ↓
generate_summary_description() [LLM调用2]
  ↓
generate_name_embedding() [Embedding]
  ↓
community.save() + edge.save()
  ↓
数据库持久化
```

### 查询流程
```
搜索请求
  ↓
name_embedding 向量搜索
  ↓
匹配相关社区
  ↓
返回社区及其成员实体
```

## 数据库Schema

### 节点类型
```cypher
(:Entity {
  uuid: string,
  name: string,
  summary: string,
  group_id: string,
  ...
})

(:Community {
  uuid: string,
  name: string,
  summary: string,
  group_id: string,
  name_embedding: vector,
  created_at: datetime,
  ...
})
```

### 关系类型
```cypher
(:Entity)-[:RELATES_TO]->(:Entity)
(:Community)-[:HAS_MEMBER]->(:Entity)
```

### 查询模式

**查询实体的社区**:
```cypher
MATCH (c:Community)-[:HAS_MEMBER]->(e:Entity {uuid: $uuid})
RETURN c
```

**查询邻居的社区（投票）**:
```cypher
MATCH (c:Community)-[:HAS_MEMBER]->(m:Entity)-[:RELATES_TO]-(n:Entity {uuid: $uuid})
RETURN c
// 在代码中统计c的出现次数
```

**查询社区成员**:
```cypher
MATCH (c:Community {uuid: $uuid})-[:HAS_MEMBER]->(e:Entity)
RETURN e
```

## 并发控制

### 信号量机制
```python
# 限制并发社区构建数量
MAX_COMMUNITY_BUILD_CONCURRENCY = 10
semaphore = asyncio.Semaphore(10)

async with semaphore:
    await build_community(...)
```

**目的**: 避免同时发起过多LLM调用导致：
- API限流
- 内存溢出
- 响应超时

### 并发点
1. **get_community_clusters**: 并发获取多个集群的EntityNode
2. **build_community**: 并发合并摘要对
3. **build_communities**: 并发构建多个社区（受信号量限制）

## 性能优化策略

### 1. 二叉树归并
- 减少LLM调用轮次：O(n) → O(log n)
- 每轮内部并发调用

### 2. 增量更新
- 避免全量重建
- 只需固定2次LLM调用
- 保持社区摘要增量式演进

### 3. 邻居投票
- 简单高效的社区分配策略
- 无需复杂计算
- 利用局部拓扑结构

### 4. 并发控制
- semaphore限制并发数
- semaphore_gather批量等待
- 平衡速度和资源消耗

## 使用场景

### 场景1: 初始化图数据库
```python
# 1. 移除旧社区
await remove_communities(driver)

# 2. 全量构建社区
community_nodes, community_edges = await build_communities(
    driver, llm_client, group_ids=None
)

# 3. 批量保存
for node in community_nodes:
    await node.save(driver)
for edge in community_edges:
    await edge.save(driver)
```

### 场景2: 添加新实体
```python
# 1. 创建并保存实体节点
entity = EntityNode(name="...", summary="...", ...)
await entity.save(driver)

# 2. 增量更新社区
community_nodes, community_edges = await update_community(
    driver, llm_client, embedder, entity
)
# update_community内部会自动保存
```

### 场景3: 更新现有实体
```python
# 1. 加载实体
entity = await EntityNode.get_by_uuid(driver, uuid)

# 2. 更新实体属性
entity.summary = new_summary
await entity.save(driver)

# 3. 刷新社区摘要
await update_community(driver, llm_client, embedder, entity)
```

## 注意事项

### 1. 图数据库适配
不同的图数据库对关系的表示不同：

**Neo4j**:
```cypher
(n)-[e:RELATES_TO]-(m)
```

**Kuzu**:
```cypher
(n)-[:RELATES_TO]-(e:RelatesToNode_)-[:RELATES_TO]-(m)
```

代码中使用 `driver.provider` 判断并使用不同的查询。

### 2. 社区演进
使用增量更新时，社区摘要会不断演进：
- 优点：保持实时性，效率高
- 缺点：可能偏离全局最优
- 建议：定期执行全量重建（如每周/每月）

### 3. 孤立节点
如果实体节点没有邻居或邻居都没有社区：
- `determine_entity_community()` 返回 `(None, False)`
- `update_community()` 直接返回空列表
- 这些节点不会被分配到任何社区

### 4. 并发安全
- 同一个社区的并发更新可能导致竞争
- 建议在应用层添加分布式锁（如Redis锁）
- 或者使用消息队列序列化更新操作

## 扩展建议

### 1. 社区分裂/合并
当前实现不支持社区分裂或合并，可以考虑：
- 定期重新检测社区
- 监控社区大小，过大时分裂
- 合并高度重叠的社区

### 2. 多级社区
支持层次化社区结构：
```
大社区
  ├─ 子社区1
  │   ├─ 实体1
  │   └─ 实体2
  └─ 子社区2
      ├─ 实体3
      └─ 实体4
```

### 3. 社区质量评估
引入模块度(Modularity)等指标评估社区质量。

### 4. 增量式标签传播
在增量场景下，不重新运行完整的标签传播，而是局部调整。

## 总结

Graphiti的社区系统是一个设计精巧的图分析模块，它结合了：
- **经典图算法**（标签传播）
- **现代AI技术**（LLM摘要生成）
- **向量检索**（embedding搜索）
- **增量维护**（高效更新）

核心优势：
1. **自动化**: 无需人工定义社区
2. **语义化**: 社区摘要和名称有明确含义
3. **实时性**: 支持增量更新
4. **可扩展**: 并发控制保证大规模场景性能

建议的学习路径：
1. ✅ 理解数据结构（CommunityNode, CommunityEdge, Neighbor）
2. ✅ 掌握标签传播算法（label_propagation）
3. ✅ 理解全量构建流程（build_communities）
4. ✅ 掌握增量更新流程（update_community）
5. 实践：在测试环境运行和调试
6. 扩展：根据业务需求定制化

希望这份文档能帮助你深入理解Graphiti的社区系统！

